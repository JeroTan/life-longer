## 1. Schema Update

- [x] 1.1 Update `schema/schema.sql` to include `google_id`, `name`, and `picture` in the `users` table definition.

## 2. Apply Schema

- [x] 2.1 Re-apply the updated `schema/schema.sql` against the development D1 database.
- [x] 2.2 Re-run the seed script to re-populate development data if needed.
